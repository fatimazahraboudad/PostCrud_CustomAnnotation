server:
  port: 0
spring:
  datasource:
    password: F@tiOuss@m@123
    username: postgres
    url: jdbc:postgresql://localhost:5432/PostDB
    driver-class-name: org.postgresql.Driver


  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        types.print.banner: false
        format_sql: true
  application:
    name: ProjectWithCustomAnnotation
